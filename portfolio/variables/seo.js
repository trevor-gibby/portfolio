// The project uses the Next.js Pages Router, so this shared value serves as
// the metadata base for canonical, Open Graph, sitemap, and structured-data URLs.
export const metadataBase = 'https://www.trevorgibby.dev'
export const defaultOpenGraphImage = `${metadataBase}/og-senior.png`
